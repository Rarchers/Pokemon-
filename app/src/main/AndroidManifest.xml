<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rarcher.pokedex">
    <!-- 得到屏幕正常的物理尺寸 -->
    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:resizeable="true"
        android:smallScreens="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pokedex">
        <activity
            android:name=".pokeDex.activity.PokeDex"
            android:label="@string/title_activity_poke_dex"
            android:theme="@style/Theme.Pokedex.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".pokeDetail.activity.PokeDetail"
            android:label="@string/title_activity_poke_detail"
            android:theme="@style/Theme.Pokedex.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Pokedex.NoActionBar">

        </activity>
    </application>

</manifest>